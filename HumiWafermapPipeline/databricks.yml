# This is a Databricks asset bundle definition for HumiWafermapPipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: HumiWafermapPipeline
  uuid: 6e79d387-cc0a-48be-9630-97dff24eb52a

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-5106020421355827.7.azuredatabricks.net
    variables:
      catalog: hive_metastore
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-5106020421355827.7.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/manuel.koch@sensirion.com to make sure we only have a single copy.
      root_path: /Workspace/Users/manuel.koch@sensirion.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: hive_metastore
      schema: prod
    permissions:
      - user_name: manuel.koch@sensirion.com
        level: CAN_MANAGE
